name: ATIS v37 Validation (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ATIS v37 validation workflow retired after successful production deploy."
