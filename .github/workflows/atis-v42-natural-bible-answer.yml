name: Validate ATIS v42 Natural Bible Answer

on:
  push:
    branches:
      - agent/atis-v42-natural-bible-answer
    paths:
      - '.github/workflows/atis-v42-natural-bible-answer.yml'
      - 'scripts/atis-v42-natural-bible-answer.py'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/atis-v42-natural-bible-answer.yml'
      - 'scripts/atis-v42-natural-bible-answer.py'

permissions:
  contents: write

env:
  SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
  SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

jobs:
  patch-test-deploy:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/atis-v42-natural-bible-answer
          fetch-depth: 0

      - name: Apply v42 natural Bible answer patch
        run: python scripts/atis-v42-natural-bible-answer.py

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Run ATIS regressions
        run: |
          deno test supabase/functions/_shared/atis/context-routing_test.ts
          deno test supabase/functions/_shared/atis/link-policy_test.ts
          deno test supabase/functions/_shared/atis/response-style_test.ts

      - name: Type-check affected functions
        run: |
          deno check supabase/functions/atis-ai/index.ts
          deno check supabase/functions/atis-webhook/index.ts

      - name: Commit validated v42
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "romulovidal"
          git config user.email "romulo_vidal@live.com"
          git add supabase/functions/_shared/atis/assistant.ts supabase/functions/_shared/atis/response-style_test.ts supabase/migrations/20260818100500_atis_natural_ask_bible_prompt.sql
          git diff --cached --check
          git commit -m "fix(atis): make Bible answers conversational by default"
          git push origin HEAD:agent/atis-v42-natural-bible-answer

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate production target
        shell: bash
        run: |
          set -euo pipefail
          test -n "${SUPABASE_ACCESS_TOKEN:-}"
          test -n "${SUPABASE_PROJECT_ID:-}"
          test "$SUPABASE_PROJECT_ID" = "karyuuhxeismshhxuokg"

      - name: Deploy ATIS functions
        run: |
          supabase functions deploy atis-ai --project-ref "$SUPABASE_PROJECT_ID"
          supabase functions deploy atis-webhook --project-ref "$SUPABASE_PROJECT_ID" --no-verify-jwt
