name: Validate ATIS v43 Bible Output Repair

on:
  push:
    branches:
      - agent/atis-v43-repair-bible-output
    paths:
      - '.github/workflows/atis-v43-repair-bible-output.yml'
      - 'scripts/atis-v43-repair-bible-output.py'
  pull_request:
    branches:
      - main

permissions:
  contents: write

env:
  SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
  SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}

jobs:
  patch-test-deploy:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/atis-v43-repair-bible-output
          fetch-depth: 0

      - name: Apply v43 Bible output repair
        run: python scripts/atis-v43-repair-bible-output.py

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Run ATIS regressions
        run: |
          deno test supabase/functions/_shared/atis/context-routing_test.ts
          deno test supabase/functions/_shared/atis/link-policy_test.ts
          deno test supabase/functions/_shared/atis/response-style_test.ts

      - name: Type-check affected functions
        run: |
          deno check supabase/functions/atis-ai/index.ts
          deno check supabase/functions/atis-webhook/index.ts

      - name: Commit validated v43
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "romulovidal"
          git config user.email "romulo_vidal@live.com"
          git add supabase/functions/_shared/atis/assistant.ts supabase/functions/_shared/atis/response-style_test.ts
          git diff --cached --check
          git commit -m "fix(atis): repair malformed Bible answers before delivery"
          git push origin HEAD:agent/atis-v43-repair-bible-output

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Validate production target
        shell: bash
        run: |
          set -euo pipefail
          test -n "${SUPABASE_ACCESS_TOKEN:-}"
          test -n "${SUPABASE_PROJECT_ID:-}"
          test "$SUPABASE_PROJECT_ID" = "karyuuhxeismshhxuokg"

      - name: Deploy ATIS functions
        run: |
          supabase functions deploy atis-ai --project-ref "$SUPABASE_PROJECT_ID"
          supabase functions deploy atis-webhook --project-ref "$SUPABASE_PROJECT_ID" --no-verify-jwt
